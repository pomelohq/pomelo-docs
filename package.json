{
  "name": "pomelo-docs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:changelog": "node scripts/fetch-changelog.mjs",
    "dev": "npm run sync:changelog && vitepress dev",
    "build": "npm run sync:changelog && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
